/** @format */

export enum ScreenType {
    PORTFOLIO = '',
    PLATFORM = 'platform',
}

export enum ScreenPlatformPath {
    GENERATOR = 'generator',
    GAME = 'game',
}

export enum GeneratorPages {
    STRING = 'string',
}

export enum GamePages {
    NAUGHTS_AND_CROSSES = 'naughts-and-crosses',
}
